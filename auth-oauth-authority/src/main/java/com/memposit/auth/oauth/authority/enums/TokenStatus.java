package com.memposit.auth.oauth.authority.enums;

/**
 * The enum Token status.
 */
public enum TokenStatus {

    /**
     * Token invalid token status.
     */
    TOKEN_INVALID,
    /**
     * Token expired token status.
     */
    TOKEN_EXPIRED,
    /**
     * Token valid token status.
     */
    TOKEN_VALID,
}
